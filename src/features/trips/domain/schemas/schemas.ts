/**
 * Vacío a propósito: el recurso de lectura de un viaje todavía no está definido
 * por el backend. Cuando publique `GET /trips` aquí entran `TripSchema` y
 * `PaginatedTripsSchema`. El `export` mantiene el archivo como módulo para que
 * `domain.ts` pueda reexportarlo.
 */
export { };
